# etl

This Python project performs the extract-transform-load on the
raw database (input) and analytics database (output).

It adds some external data to the dataset:

- For each trip, a weather condition is added
- For each trip, a pickup and dropoff city is determined
